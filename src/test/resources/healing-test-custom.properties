# Used only by HealingConfigTest to prove a classpath properties file's value
# is honored when no system property overrides it. Deliberately differs from
# every hardcoded default in HealingConfig.
healing.enabled=false
healing.llm.provider=ollama
